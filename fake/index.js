import TextComp from './lib/text';
import number from './lib/number';
import A, { B, C } from './lib/form';

import './lib/no';

Vue.use(TextComp);
const test = {
  TextComp
};
test111(TextComp);

Vue.use(B);
